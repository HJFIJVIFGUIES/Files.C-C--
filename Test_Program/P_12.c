#include <stdio.h>
int main()
{
    // int a;
    // char b;
    // float c;
    // double d;

    //without variable to get the sizeof() of the datatypes.
    int; char; float; double;

    // Using sizeof() operator to get the size of datatypes.
    printf("%d Byte\n", sizeof(int));
    printf("%d Byte\n", sizeof(char));
    printf("%d Byte\n", sizeof(float));
    printf("%d Byte\n", sizeof(double));
    return 0;
}